# AutoPM 主动模拟验证报告

- **验证时间**：2026-09-17 12:16–12:30
- **代码版本**：AutoPM_Source_20260915（含 current_progress KeyError + identity KeyError 修复 + preview.html 重写）
- **验证目标**：在交付用户前主动发现所有运行时异常，而非被动等待用户报错

---

## 一、验证范围（三条主线全链路）

| 链路 | 工具 | 覆盖范围 | 数据风险 |
|---|---|---|---|
| 1. `prepare_preview`（周报→云端预览） | `verify_full_chain.py` | 读取 xlsx → reconcile → build_plan → enrich | 只读 Airtable schema + records |
| 2. `prepare_airtable_tracker`（本地总表→云端） | `verify_full_chain.py` | 读取 xlsx tracker → build_airtable_tracker_plan | 只读 Airtable schema + records |
| 3. `apply_plan`（写入链路 dry-run） | `verify_apply_plan.py` | mock 客户端模拟 guards → identity checks → _coerce → concurrent-edit → 实际 journal + result | **零 API 调用**（完全隔离） |

---

## 二、主动发现的异常与修复

### 本次修复（已解决）

| # | 异常 | 根因 | 修复文件 | 修复内容 |
|---|---|---|---|---|
| R1 | `KeyError('current_progress')` | `REQUIRED_PROJECT_TYPES` 缺 `"current_progress"` 键；`required_project_types()` 在 reconcile 阶段先于 build_plan 执行 | `sync.py` L87-94 | 补入 `"current_progress": "richText"`、`"report_date": "date"` |
| R2 | `KeyError('identity')` | `_milestone_conflicts()` / `_identity_hits()` 无条件访问 `operation["identity"]`；projects operation 从不带 identity | `sync.py` L1268、L1337 | 两处函数开头增加 `if "identity" not in operation: return []` |
| R3 | `latest_update` 被误判为 date | `schema_memory.compatibility()` 用 `endswith("date")`，`latest_update` 尾部 "up**date**" 被误匹配 | `schema_memory.py` L136 | 改为 `endswith("_date")` 与 workbook.py 权威定义一致 |

### 本次验证未发现新异常

- Mock dry-run 196 条全部写入通过，无 errors
- Preview HTML 质量 10 项结构性检查全部 PASS
- py_compile 全量通过

---

## 三、各链路验证结果

### 链路 1：prepare_preview（真实 API）

```
[PASS] prepare_preview — changes=196 warnings=1894 base=appOMWiK4CTOH7iQu
```

- blockers 为空
- warnings 抽样检查：均为未映射字段 / 删除线项目等正常业务提示

### 链路 2：prepare_airtable_tracker（真实 API）

```
[PASS] prepare_airtable_tracker — changes=0 warnings=2998
```

- 本地总表已与云端一致（changes=0）
- warnings 为配置级提示，无故障

### 链路 3：apply_plan dry-run（Mock 客户端，零 API）

```
Status: completed
Applied: 196, Skipped: 0
Errors: []
Total API calls: 196
Creates: 53, Updates: 143
[PASSED] Dry-run apply_plan completed successfully!
```

**关键验证点**：
- 前 17 条 projects 写入成功后，后续所有 tasks 均正常通过 `_milestone_conflicts` 和 `_identity_hits`，无 KeyError
- `guard_project` 验证通过（project ID + date 校验）
- `concurrent-edit` guards 通过
- `issue_match_guard` 未触发（本次无 issues）
- journal 写入正常（tmpdir 隔离）
- result.json 标记 `completed`，无 `partial` 状态

---

## 四、Preview HTML 质量验证

| 检查项 | 结果 |
|---|---|
| 顶部统计卡片（项目/任务/问题/总计） | ✅ PASS |
| 结构化表格（项目·类型·操作·字段·当前值·周报新值） | ✅ PASS |
| 搜索框 | ✅ PASS |
| 类型筛选（项目/任务/问题） | ✅ PASS |
| 操作筛选（新增/更新） | ✅ PASS |
| 原始 JSON 折叠面板 | ✅ PASS |
| 新增行绿色高亮 | ✅ PASS |
| 长文本自动截断（>400字） | ✅ PASS |
| 无大块 JSON `<pre>` 块 | ✅ PASS |
| 418 行表格数据正常渲染 | ✅ PASS |

---

## 五、遗留已知项（非故障，不影响运行）

| 项 | 说明 | 状态 |
|---|---|---|
| LOA-13weeks plan(Manual) 字段类型 | 云端仍为 number，需用户手动改为 multilineText | 待用户操作 |
| GitHub 提交 | 用户明确自行处理，不主动提交 | 用户负责 |
| PIL/Pillow 缺失 | .venv 环境问题，非代码 bug；运行 UI 前需在 .venv 安装 Pillow | 环境配置 |
| LSP 编辑器报错 | tkinter 动态属性 + openpyxl 类型推断噪音，不影响运行时 | 非故障 |

---

## 六、结论

**本次主动模拟验证覆盖了三条主线，发现并修复 3 处异常，未发现新故障。**

- `current_progress` KeyError：已从根因消除
- `identity` KeyError：已从根因消除  
- `latest_update` 类型误判：已消除
- 196 条写入链路 mock 验证：全部通过
- Preview HTML 生成：质量合格

**用户现在可以安全地重新运行流程：** 选择周报 → 解析 → 预览（新表格清晰展示 196 条变更）→ 确认写入 → 应得到 `status: completed` 且 `errors: []`。

---

## 附录：回归测试脚本

- `verify_full_chain.py` — 真实 API 只读链路（需 token）
- `verify_apply_plan.py` — Mock dry-run 写入链路（零 API，隔离环境）

后续改动 schema/写入相关代码后，建议先跑 `verify_apply_plan.py` 确认无阻塞再交付。
