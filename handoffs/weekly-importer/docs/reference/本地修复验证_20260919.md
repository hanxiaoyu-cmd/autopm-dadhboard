# 本地源码修复与验证（2.4.0-rc4）

## 本次故障与修复

9 月 19 日的写入日志与两条云端记录表明：SXA0344、NXA0196 的 Engineering remark 已写入，Airtable 自动调整了 Markdown 列表编号并增加末尾换行，原程序逐字比较后误报失败。继续导入又可能重复追加同日进展。

本次在共同的富文本比较函数中兼容列表编号格式，并让周报合并、All Tracker 回写共用该规则。正文、日期、数量、转义数字和代码中的数字仍严格比较。真实内容不同仍会停止；日志保留记录 ID 和具体字段差异，界面区分“已返回写入成功但校验未通过”和“未写入”。保留现有历史，没有删除云端重复段落。

## 测试方式与证据

- 全量自动测试：428 项通过，日志 `.local/rc4_fix/full-tests.log`。
- 源码界面启动：通过；直接读取 20260918 两份原始 Excel，Shark 185 个项目块、Ninja 176 个项目块。
- 当前真实 schema 的新配置映射：无阻塞项。
- 两条故障记录在线只读核对：已有写入内容与修复后的比较规则一致。
- 导入、导出、回写使用原始运行日志中的真实解析结果和云端快照，在本地副本回放；模拟已观察到的 Airtable 富文本规范化。此次未向正式 Airtable 新增测试写入。

## 使用

关闭旧版本，双击本目录的 `启动源码版.cmd`，或桌面的“AutoPM周报工具-本地源码版”。重新选择周报并生成预览，再核对写入内容。此次没有重新打包 EXE。

## 验证边界

本次验证针对写入误报、重复追加、导出回写与冲突保护，不代表周报里所有警告都已消除。无法确认的人员、工厂、项目或里程碑仍按现有规则提示或跳过。Project 级来源与 Airtable SKU 级管理的完整业务一致性，以及 Project/Task 双处日期的全面同步，不属于本次故障修复的完成声明。

Ninja 旧预览之后云端新增了 EB Build 选项，因此当前结构下重建预览可能多出一条 EB1 → EB Build 分类变更；这不是重复任务匹配。该变更仅在本地回放，未写入正式库。

## 两份真实日志本地回放结果

| 检查 | Shark | Ninja |
|---|---:|---:|
| 旧预览操作全部完成 | 83 | 168 |
| 当前结构新预览操作全部完成 | 82 | 168 |
| 重复执行旧计划新增写入 | 0 | 0 |
| 新预览导入后再次预览的变化 | 0 | 0 |
| 导出 All Tracker 更新单元格 | 413 | 413 |
| 未改动总表回写变化 | 0 | 0 |
| 修改备注和 EB1 日期回写成功 | 2 | 2 |
| 再次回写变化 | 0 | 0 |
| 本地与云端同时修改时拦截 | True | True |

Shark 新预览比旧计划少 1 项，因为已经存在的同日进展不再重复追加。详细机器结果见 `.local/rc4_fix/user-runs-verification.json`；可运行检查脚本为 `.local/rc4_fix/verify_user_runs.py`。
