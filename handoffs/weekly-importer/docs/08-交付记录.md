# 周报导入工具移交包交付记录

任务 ID：WEEKLY-IMPORTER-HANDOFF-20260921
提交版本：本文件所在 Git 提交
执行者：Codex
开始／结束时间：2026-09-21（Asia/Shanghai）

目标环境：Windows 本地工具；Airtable Base `appOMWiK4CTOH7iQu`；周报 Excel；All Tracker Excel
代码分支：`codex/weekly-importer-handoff-20260921`
授权范围：整理并备份周报导入工具的技术资料、源码副本、匹配关系、完成情况、测试证据和接手步骤；不得提交 Token、真实项目数据或本地凭据。

## 业务结果

本交付把周报导入工具整理为一个独立、可直接移交的开发资料包。接手者从根目录 `README.md` 开始阅读，即可了解：

1. 工具为什么存在，以及正式业务链路：周报 → Airtable → All Tracker；All Tracker → Airtable 只用于复验和查漏。
2. Project、Task、Issue、Milestone、SKU、Factory 和 All Tracker 的管理边界。
3. 当前程序实际怎样识别项目、怎样匹配 Airtable、怎样拆分和写入记录、遇到冲突怎样阻断。
4. 最终业务规则与当前实现之间尚未闭环的差距。
5. 已完成的功能和测试、尚未完成的功能、已知故障、修复优先级与验收方法。

本次没有修改生产 Airtable、真实周报或真实 All Tracker，也没有声称导入逻辑已经最终业务验收。当前工具源码的自动化回归测试通过，但仍有两个需要下一位开发者优先处理的 P0：

- All Tracker 中异常 Excel 时间值会导致 `Object of type time is not JSON serializable`，阻断 Airtable → All Tracker 预览。
- 当前周报匹配实现会按项目编号 + SKU + 工厂拆分到 SKU 单元；这与用户最终确认的“周报只管理 Project，SKU 维度从 Airtable 开始”存在语义差距。

## 变更对象

| 对象／文件 | 原状态 | 新状态 | 消费者／影响 | 恢复办法 |
|---|---|---|---|---|
| `handoffs/weekly-importer/README.md` | 不存在 | 新增统一入口和阅读顺序 | 接手开发者可快速建立完整上下文 | 删除整个移交目录 |
| `handoffs/weekly-importer/docs/` | 资料分散在源码、历史报告和会话中 | 新增 8 份结构化文档 | 明确目标、规则、映射、架构、完成度和接手顺序 | 删除整个移交目录 |
| `handoffs/weekly-importer/source/` | 仅存在于本地工作目录 | 复制当前源码、测试、构建和启动脚本 | 接手者可独立检查和运行 | 删除整个移交目录；原工作目录不受影响 |
| `handoffs/weekly-importer/source/README.md` | 原链接依赖源目录外部文档 | 调整为指向移交包内文档 | 避免交接后入口失效 | 从原工作目录重新复制该文件 |
| `handoffs/weekly-importer/source/_verify_runtime.py` | 自检脚本复制了一份过期日期解析逻辑，出现假失败 | 改为直接调用生产代码 `_extract_narrative_date` | 自检与实际实现一致 | 从原工作目录重新复制该文件 |
| `handoffs/weekly-importer/docs/reference/` | 关键审计报告分散 | 复制筛选后的技术报告 | 保留历史判断和证据边界 | 删除该目录 |
| `handoffs/weekly-importer/legacy-reference/` | 旧版工具只存在于原本地目录 | 复制为只读参考，排除配置、日志和凭据 | 可追溯旧实现，但不作为现行源码 | 删除该目录 |

## 验收用例

| 用例 ID | 数据类型与样本版本 | 操作／角色 | Expected | Actual | 结果 | 证据路径／时间 |
|---|---|---|---|---|---|---|
| H01 | 当前源码测试集，版本 `2.4.0-rc4` | 在移交副本运行完整 `unittest` 回归 | 全部测试通过 | 428 项通过，耗时 53.149 秒 | Pass | `docs/05-完成情况测试与已知问题.md`，2026-09-21 Asia/Shanghai |
| H02 | 移交包源码副本 | 运行 `_verify_runtime.py` | 自检通过 | 全部检查通过 | Pass | `source/_verify_runtime.py`，2026-09-21 Asia/Shanghai |
| H03 | Airtable Token Helper 浏览器扩展 | 运行 Node 原生测试 | 扩展安全和输入校验测试通过 | 27 项通过，0 项失败 | Pass | `source/browser-extension/airtable-token-helper/tests/core.test.mjs`，2026-09-21 Asia/Shanghai |
| H04 | 移交包全部 Markdown | 校验相对链接 | 链接均可解析 | 29 个 Markdown 文件，0 个缺失链接 | Pass | 移交目录，2026-09-21 Asia/Shanghai |
| H05 | 移交包全部文件 | 扫描 Token、Authorization 和常见密钥模式 | 不包含真实凭据 | 只发现示例占位符和测试内明确标注的假 Token；无真实凭据 | Pass | `source/tests/`、扩展测试和示例 README，2026-09-21 Asia/Shanghai |
| H06 | `All Projects Tracker V2 (1).xlsx` | Airtable → All Tracker 读取预览 | 正常生成预览，或对异常单元格给出可定位提示 | `ALL PROJECTS!AC695` 被读为 `datetime.time(0,0,15,231000)`，内容哈希 JSON 序列化失败 | Fail | `docs/05-完成情况测试与已知问题.md`；原运行日志 `20260919_233448_e920c5` |
| H07 | 最终业务匹配原则 | 对照当前实现和用户确认规则 | 周报只落到 Project；SKU/Factory 从 Airtable 开始管理 | 当前实现仍会将项目块展开到 SKU 单元 | Fail | `docs/02-数据流与匹配规则.md`、`docs/06-接手开发顺序.md` |
| H08 | 生产 Airtable / 真实 Excel 写入 | 执行全链路写入并读回 | 写入、跳过、冲突和读回均可审计 | 本次移交任务未执行生产写入 | Not Run | 需要接手者完成 P0 修复后，以 dry-run 和小样本验证 |

## 数据与后台结果

- Git 备份文件：152 个，总计约 18.31 MB。
- 包含：现行源码、自动化测试、浏览器扩展、构建／启动辅助文件、参考报告、旧版参考代码和 8 份移交文档。
- 排除：Airtable Token、真实项目工作簿、真实导入导出结果、运行日志、`.venv`、`.local`、构建产物、EXE、缓存和本地状态文件。
- 生产数据创建／更新／删除：0／0／0。
- Airtable 写入：未执行。
- All Tracker 写入：未执行。
- 结果未知：生产端到端验收仍未运行；必须在 P0 修复后执行。

## 剩余问题与交接

| 问题 | 严重程度／影响 | 复现与证据 | 所需动作／依赖 | 建议负责角色 |
|---|---|---|---|---|
| Excel `datetime.time` 不能 JSON 序列化 | P0；Airtable → All Tracker 预览被完全阻断 | `All Projects Tracker V2 (1).xlsx` 的 `ALL PROJECTS!AC695`；错误为 `Object of type time is not JSON serializable` | 在 `autopm/tracker.py::scalar()` 统一处理 `datetime.time`；增加单元测试并重跑真实文件 | Python 开发者 |
| 周报导入仍按 SKU/Factory 展开 | P0；可能把 Project 级信息错误复制到多个 SKU 记录 | 当前 `autopm/sync.py` 的匹配和 fan-out 路径；见规则文档 | 先把 Project 级写入与 SKU 级管理边界写成测试，再以最小改动调整匹配 | 业务规则负责人 + Python 开发者 |
| Project 和 Task 分表规则需真实样本验收 | P1；自动测试通过不等于业务字段映射正确 | 见 `docs/03-字段映射与分表规则.md` | 使用 9/19 两个样本做 preview，逐字段审核后才写入 | 业务验收人 |
| All Tracker 双向回写的字段所有权需严格限制 | P1；错误回写可能覆盖 Airtable 人工维护内容 | 见 `docs/02-数据流与匹配规则.md` | 只允许大节点日期和明确白名单字段；冲突默认阻断 | 业务规则负责人 |
| 企业网络 TLS 信任链 | P1；部分公司电脑 Python 请求失败，但浏览器可访问 Airtable | 网络报告显示 `SSLCertVerificationError` | 保留系统证书兼容方案，禁止关闭 TLS 校验；在公司电脑复测 | IT / Python 开发者 |

维护入口、配置位置、运行和恢复步骤均写在 `README.md`、`docs/04-架构运行与安全.md` 和 `docs/06-接手开发顺序.md`。下一位开发者应严格按 `docs/06` 的 P0 顺序推进，每一次真实写入前先生成 preview，并保留记录 ID、字段级差异、写入结果和读回结果。

执行状态：Submitted。业务验收状态：Pending，等待 P0 修复和真实小样本端到端验收。

## 用户确认记录

确认日期／时区：2026-09-21，Asia/Shanghai
确认范围与允许修改对象：用户要求“把周报导入任务梳理一下，把所有资料备份到 GitHub，并单独建子文件夹；包含所有技术资料，尤其是脚本匹配关系；接手者拿到后应知道任务目标、已完成内容、后续做法和匹配关系，尽可能详细”。
确认出处：当前 Codex 会话中的用户明确授权。
执行身份与起始提交：Codex；`29eead8`。
范围是否变化：用户第二次说明需要尽可能详细；交付范围相应增加为完整开发移交包，无需修改生产数据。

## 执行日志与提交

| 时间／时区 | 动作 | 实际结果／证据 |
|---|---|---|
| 2026-09-21 Asia/Shanghai | 核对现行源码、历史资料、运行日志和业务规则 | 形成 7 份专题文档和统一入口 |
| 2026-09-21 Asia/Shanghai | 复制现行源码和筛选后的参考资料 | 152 个文件，约 18.31 MB；排除凭据和真实数据 |
| 2026-09-21 Asia/Shanghai | 在移交副本运行完整回归测试 | Python 428 项通过；浏览器扩展 27 项通过 |
| 2026-09-21 Asia/Shanghai | 运行移交包运行时自检 | 全部通过 |
| 2026-09-21 Asia/Shanghai | 检查相对链接、暂存内容和密钥模式 | 29 个 Markdown 文件链接有效；Git 未包含 `.pyc`；无真实凭据 |

原 Issue：无；本任务来自用户直接授权。
结果分支：`codex/weekly-importer-handoff-20260921`。
实际变更见上方“变更对象”；待接手方业务验收，不自行宣称 P0 闭环。

## 本轮检查点

attempt：1
本轮开始／结束时间及时区：2026-09-21，Asia/Shanghai
起始提交：`29eead8`
本轮结束提交：见本文件所在提交
本轮完成：独立移交目录、源码副本、规则文档、状态和问题清单、自动化验证。
未验证：生产 Airtable 写入、真实 All Tracker 写入、公司网络环境。
阻塞：无 Git 交付阻塞；业务上线受两个 P0 问题阻挡。
原 Issue 回报评论 URL：无。
下一步：由接手开发者先修复 `datetime.time` 序列化，再固化 Project/SKU 管理边界，最后完成小样本 dry-run、写入和读回验收。
写回失败的待补报文件：无。
