# 给执行 AI：微软离线首发工作包

使用方式：把开发控制包ZIP或目录给执行AI，再提供包内列出的批准源码/测试样例。没有本地目录访问时，以最小源文件包补足；不要把个人配置和全部数据上传。本轮没有实际派发。

---

你是AutoPM的微软与集成工程执行者。本次只承接 **MS-01：复用Bridge并分离公共规则**。Dataverse尚未取得；这次交付是可复用的离线处理工程和接口契约，不能声称已经建立正式Power Apps/Dataverse应用。

先读包内 `README.md`、`01_BLUEPRINT.md`、`tasks/MS-01.md`、`03_ACCEPTANCE.md`、`reviews/RV-20260911-001.md`；再读 `sources/bridge-handover.md`、`prd-text.md`及字段规划。

源码位置索引（有本地访问时使用，否则由共享版本提供最小文件集）：

- 既有Bridge：`D:/个人资料/AI学习圈/SN Auto PM/07-脚本与数据治理 Scripts & data ops/sync_bridge/`。只读取需要的解析、映射、输出和测试文件；**不读取或复制sync_config.json等个人凭据配置**。
- 候选工程：`C:/Users/40734/Documents/Codex/autopm-dual-platform-p0/integrations/bridge/`、`autopm/domain.py`及相关测试。它们未通过完整平台验收；旧测试结果要按实际文件版本复现。
- Shark/Ninja周报、计划附件、Tracker回归样例：使用团队批准的固定样例及哈希，缺文件记录最小需求，不伪造真实导入结果。

工作步骤：

1. 盘点已有解析、匹配、业务规则、Airtable写入、Excel输出和测试边界，形成复用图。先运行可执行的离线测试并固化原结果。
2. 定义目标无关变更计划：source identity、source version、目标业务键、匹配依据、操作类型、旧/新值、预期版本、请求标识、错误/冲突/未知结果。Task/Issue描述改名不应变成新对象。
3. 按BR契约分离解析与写入适配器；来源绿色只代表报告声称完成；未知单日期不默认加7天；空值不默认清空；创建和更新均有独立读回接口。Airtable与Dataverse适配器共用前层，保留平台差异。
4. 复现并补齐TC-04/15/16/17/18/19/30；特别覆盖创建已成功但响应丢失、重复导入、预览冲突、公式/Baseline/原文件保护。提交实际机器可读日志，不只报测试数量。
5. 输出公共模块、适配器契约、固定输入/预期变更计划、差异说明及部署待办，按提交模板放 `submissions/MS-01/v1/`。列哪些离线验证通过、哪些等待实际Dataverse平台。

限制：不连接生产写入、不修改当前SharePoint MVP、不申请广泛租户权限、不建未批准托管服务、不发送通知、不修复与本包无关的所有模块。SourceRecordMap元数据重复字段已登记给MS-02；不要为修这一缺陷扩展本包为整个微软平台建设。

结束提交 **Submitted**；由协调方验收。若只能读到文档，先完成边界/契约和缺失源文件清单，明确“离线实现未完成”，不能把设计当已编码成果。
