# All Tracker 时间值修复交付

任务 ID：TRACKER-TIME-20260921；执行者：Codex；日期：2026-09-21（Asia/Shanghai）。
目标：Windows 本地周报导入工具，源码 `handoffs/weekly-importer/source`。
分支：`codex/weekly-importer-handoff-20260921`；起始提交：`0976ee8aa2ad6e932e6c3a75cadaa52badf56a11`；交付版本：本文件所在提交，应用版本 `2.4.0-rc5`。

## 业务结果

已修复 Excel 时间／时长造成预览、导出审计 JSON 失败的问题；异常日期格可定位，完整云端日期写入后按日期显示。用户指定总表已生成更新副本。完整依据见 [更新日志](../../../../../handoffs/weekly-importer/CHANGELOG.md)。

## 变更对象

| 文件 | 原状态 | 新状态／影响 | 恢复办法 |
|---|---|---|---|
| `source/autopm/tracker.py` | 时间与时长不可序列化 | 稳定文本和日期列诊断，保留既有完整日期规则 | 回退本次源码提交 |
| `source/autopm/native_xlsx.py` | 纯时间格式被当成完整日期格式 | 仅实际更新的日期格补完整日期格式 | 同上 |
| `source/autopm/tracker_roundtrip.py` | 回写预览不继承读取警告 | 继承日期列诊断 | 同上 |
| `source/tests/test_tracker_temporal_values.py` | 无专项用例 | 新增 7 项合成回归 | 同上 |
| 版本与交接文档 | rc4／历史 rc3 描述 | rc5，注明历史与本次实测边界 | 同上 |

上述 `source/` 路径均相对于 `handoffs/weekly-importer/`。

## 验收用例

| 用例 | 输入／操作 | 期望 | 实测 | 结果 |
|---|---|---|---|---|
| T01 | 合成 time、跨天／负 timedelta、日期样式零值 | 精度可保留，指纹与预览可序列化 | 原始提交复现失败，rc5 通过 | Pass |
| T02 | 空项目行及有效行的 FOT/EB1 时间 | 定位异常，不猜日期；旁路格不变 | 警告及原 XML 验证通过 | Pass |
| T03 | 云端日期更新纯时间／累计时长格式格 | Excel 显示完整日期 | 两种格式子用例通过 | Pass |
| T04 | 完整 Python 回归 | 无回归 | 435 项通过 | Pass |
| T05 | 运行时与桌面自检 | 当前源码可启动 | 两项通过 | Pass |
| T06 | 只读快照 → 用户指定总表副本 | 实际本地写出，保留原表及公式 | 406 格更新；原表未变；19,505 公式保留 | Pass |
| T07 | 重复导出／未编辑回写预览 | 0 变化 | 两者均 0；回写 0 阻断 | Pass |
| T08 | 历史 V2 故障原文件 | 原文件复测 | 本轮未提供，异常类型以合成样本复现 | Not Run |
| T09 | 真实 Airtable 写入 | 本次不需要 | 仅 GET，不执行写入 | Not Run |

## 数据与后台结果

本轮只读采集 169 次 GET。当前源码使用该快照进行本地验证：399 个本地项目行中选入 135 行，406 格更新，1,447 条回写基线；其余行维持原值。现有映射／匹配提示保存在本地审计，不提交业务数据。原始输入、快照、配置、日志和凭据未进入提交。无后台自动化。

## 剩余问题与交接

- 项目编号＋SKU＋工厂及周报版本规则保持，未匹配行需按现有提示核对；Project/SKU 模型重构另行处理。
- Excel 打开副本后需刷新公式、透视表及外部链接；4 个公式缓存待 Excel 重算。
- 使用 `handoffs/weekly-importer/source/启动源码版.cmd` 启动 rc5；本地依赖已安装，现有加密设置仅在本机复用。原总表保持不变，副本和同名 `.autopm.json` 应一起保留。
- 回退：回退本次提交即可恢复代码，使用未覆盖的原表恢复本地输入。无云端回滚步骤，因为未写云端。

执行状态：Submitted；业务模型与真实云端写入验收不在本次交付范围。

## 用户确认记录

2026-09-21，Asia/Shanghai；来自当前会话的直接授权：修复 Airtable 无法更新本地 All Tracker，提供原表位置，并明确指定本分支 `handoffs` 中的真实程序；同时要求源码、测试及更新日志同步 GitHub。没有扩大云端写入或业务匹配范围。

## 执行日志与检查点

| 时间／时区 | 动作 | 实际结果 |
|---|---|---|
| 2026-09-21 Asia/Shanghai | 按用户纠正的分支重新核对实现 | 在隔离 checkout 修复真实程序；旧根目录试验改动已撤回 |
| 同日 | 合成复现、修复与回归 | 新增 7 项及全量 435 项通过 |
| 同日 | 当前源码导出用户表副本 | 更新、公式、原表指纹、重复导出及回写预览验证通过 |
| 同日 | 本地部署 | 独立虚拟环境、加密设置复用、桌面启动自检通过 |

原 Issue：无，用户会话直接指定修复；未新建 Issue 或发布对外消息。结果分支见页首，提交以本文件所在版本为准。attempt：1；无功能修复阻塞；原始 V2 文件和真实云端写入未复测。
